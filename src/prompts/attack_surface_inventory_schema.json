{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://local.codex/high-risk-modules-schema.json",
  "title": "High-risk module inventory",
  "type": "array",
  "minItems": 1,
  "items": {"$ref": "#/$defs/module_record"},
  "$defs": {
    "module_record": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "is_high_risk", "code_dir", "reason"],
      "properties": {
        "name": {"type": "string", "minLength": 1, "pattern": "^\\S(?:.*\\S)?$"},
        "is_high_risk": {"type": "boolean"},
        "code_dir": {
          "type": "string",
          "minLength": 1,
          "pattern": "^(?:\\.|(?![A-Za-z]:)(?!\\./)(?!.*(?:^|/)\\.{1,2}(?:/|$))(?!.*\\\\)[^/]+(?:/[^/]+)*)$"
        },
        "reason": {"type": ["string", "null"]}
      },
      "allOf": [
        {
          "if": {"properties": {"is_high_risk": {"const": true}}},
          "then": {"properties": {"reason": {"type": "string", "minLength": 1, "pattern": "^\\S(?:.*\\S)?$"}}},
          "else": {"properties": {"reason": {"type": "null"}}}
        }
      ]
    }
  }
}
