在外部高风险模块 `${module_name}` 中找到恰好一个此前未被发现的/未在已知问题列表中记录的 DoS漏洞，该漏洞需要可通过外部消息触发，模块目录为 `${code_dir}`

## 完成标准（缺一不可）
1. 存在从外部消息入口可达的真实代码路径。拒绝L金存在与内部函数，debug/测试专用接口、无法被外部消息触达的代码
2. 触发后有可观测的DoS效果（进程崩溃、死循环、状态机死锁、资源耗尽、空指针、越界、逻辑错误、资源抢占、放大攻击等能够持续阻断服务的异常），并能说明影响范围
3. 给出最小复现整数：确切的消息类型、非标准配置或编译选项

只允许生成漏洞报告 `${output_dir}/${report_filename}`，不要修改目标代码。

报告必须是 UTF-8 Markdown，并包含以下内容：

```markdown

## 模块信息
模块名称和代码目录。

## 漏洞概述
DoS 问题的简要说明。

## 外部触发路径
从攻击者可控外部消息到 DoS 影响的完整步骤。

## 代码根因
缺陷代码及产生问题的原因。

## DoS 影响
崩溃、阻塞、资源耗尽或服务不可用等具体影响。

## 代码证据
列出仓库相对文件路径、代码符号、行号及说明。

## 触发条件
利用该问题所需的状态、权限、消息内容或调用次数。
```

只有找到上述 DoS 问题并生成该 Markdown 报告后，任务才算完成。
